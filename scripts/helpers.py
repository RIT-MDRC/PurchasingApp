'''
Date: 05/06/2019
Developer: Andrew Serrra
Description: Contains the functions that are simplifying the process
'''

def parseCommands(data_text):

    data_text = list(data_text.split())

    # input_text = {}
    #
    # for i in range(len(data_text)):
    #     if data[i][:2] == "--":
    #         input_text["commands"].append(data[i][2:])
    #     else:
    #         input_text["inputs"] = data[i:]
    #         break

    return data_text
